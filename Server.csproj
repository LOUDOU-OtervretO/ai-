<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net7.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>AiDiagServer</AssemblyName>
  </PropertyGroup>
  <ItemGroup>
    <!-- No extra NuGet required for basic HttpClient usage; add packages later if needed -->
  </ItemGroup>
</Project>
